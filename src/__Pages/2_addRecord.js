//TO-DO ====================================================================================================================
//add the dropdown to select patient, that is gonna be sent to the form so we can fetch which patient we want to check the records from

import Form from "../components/2_AddRecord";

const AddRecord = () => {
  return (
    <>
      <h1>ADD RECORD</h1>
      <Form />
    </>
  );
};

export default AddRecord;
